info:
  name: Buat user
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/users"
  body:
    type: json
    data: |-
      {
        "nama": "Budi Santoso",
        "username": "budi",
        "password": "password123",
        "phone": "081234567890",
        "alamat": "Bandung",
        "role_id": 7
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
