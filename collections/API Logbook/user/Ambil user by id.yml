info:
  name: Ambil user by id
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/users/:id"
  params:
    - name: id
      value: "6"
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
