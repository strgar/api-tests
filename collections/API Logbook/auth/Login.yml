info:
  name: Login
  type: http
  seq: 1

http:
  method: POST
  url: "{{baseUrl}}/api/auth"
  auth: inherit

runtime:
  variables:
    - name: baseUrl
      value: http://localhost:3000/

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
