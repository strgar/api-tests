info:
  name: get-category-id
  type: http
  seq: 3

http:
  method: GET
  url: "{{baseUrl}}/kategori_berita/3"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
