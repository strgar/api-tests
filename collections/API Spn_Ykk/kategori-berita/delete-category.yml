info:
  name: delete-category
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/kategori_berita/3"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
