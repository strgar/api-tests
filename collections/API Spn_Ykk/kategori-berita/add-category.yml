info:
  name: add-category
  type: http
  seq: 2

http:
  method: POST
  url: "{{baseUrl}}/kategori_berita"
  body:
    type: json
    data: |-
      {
        "nama": "Sepak Bola"
      }
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
