info:
  name: get-category
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/kategori_berita"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
