info:
  name: delete-berita
  type: http
  seq: 5

http:
  method: DELETE
  url: "{{baseUrl}}/berita/:id"
  params:
    - name: id
      value: ""
      type: path
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
