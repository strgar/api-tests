info:
  name: get-berita
  type: http
  seq: 1

http:
  method: GET
  url: "{{baseUrl}}/berita"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
