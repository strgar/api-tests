info:
  name: add-berita
  type: http
  seq: 3

http:
  method: POST
  url: "{{baseUrl}}/berita"
  auth:
    type: bearer
    token: "{{token}}"

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
